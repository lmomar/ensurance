{% extends 'FOSUserBundle::layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Ajouter un groupe</h1>
        </div>
    </div><!--/.row-->


    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">Nouvel Groupe</div>
                <div class="panel-body">
                    <div class="col-md-6">

                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <input type="submit" value="Enregistrer" />
                        {{ form_end(form) }}
                    </div>
                </div>
            </div><!-- /.col-->
        </div><!-- /.row -->
        <ul>
            <li>
                <a href="{{ path('admin_groupe_index') }}">Back to the list</a>
            </li>
        </ul>
    {% endblock %}
